/*
Created by Franz Zemen 02/22/2024
License Type: MIT
*/

import {Log} from "../log/index.js";

function getPackageConfig() {
  const log:Log = new Log();
  log.info('Getting package.json ...\n');
  log.info(`merge the following into your top-level package.json.  Place the "build-system" key in the root of the package.json file.`);

}
